#include <string.h>

#include "sdk.h"

int main(int argc, char* argv[]) {
  if (argc == 1) {
    return 0;
  }
  if (strcmp(argv[1], "get") == 0) {
    // return ret_str(NAME);
    return 0;
  }

  if (strcmp(argv[1], "set") == 0) {
    // return ret_str(SYMBOL);
    return 0;
  }
  return 0;
}
